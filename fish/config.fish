if status is-interactive
    # Commands to run in interactive sessions can go here
    set PATH $PATH /usr/lib/psql12/bin
    set PATH $PATH /home/adrian/.local/bin
    set PATH $PATH /home/adrian/.mix/escripts
    set PATH $PATH /home/adrian/.cargo/env
    set PATH $PATH /home/adrian/.cargo/bin
end
